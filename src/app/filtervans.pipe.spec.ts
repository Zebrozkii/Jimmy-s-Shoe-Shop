import { FiltervansPipe } from './filtervans.pipe';

describe('FiltervansPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltervansPipe();
    expect(pipe).toBeTruthy();
  });
});
