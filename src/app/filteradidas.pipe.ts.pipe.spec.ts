import { Filteradidas.Pipe.TsPipe } from './filteradidas.pipe.ts.pipe';

describe('Filteradidas.Pipe.TsPipe', () => {
  it('create an instance', () => {
    const pipe = new Filteradidas.Pipe.TsPipe();
    expect(pipe).toBeTruthy();
  });
});
